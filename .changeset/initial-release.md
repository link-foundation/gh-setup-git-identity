---
'gh-setup-git-identity': minor
---

Initial release of gh-setup-git-identity CLI tool

- Setup git identity (user.name and user.email) based on authenticated GitHub user
- Support for global and local git configuration
- Dry-run mode to preview changes without applying them
- Automatic prompt to login if GitHub CLI is not authenticated
- Verbose mode for debugging
